machine:
  yarn:
    version: 0.24.6
  environment:
    PATH: "${PATH}:${HOME}/${CIRCLE_PROJECT_REPONAME}/node_modules/.bin"

branches:
  ignore:
    - gh-pages

dependencies:
  override:
    - yarn run install:dev
  cache_directories:
    - ~/.cache/yarn

test:
  override:
    - yarn test
